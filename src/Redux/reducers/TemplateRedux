import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  token: "",
  isLogin: "",
  user: "",
};

const namaReduxnya = createSlice({
  name: "loginAuth",
  initialState,
  reducers: {},
});

export default namaReduxnya.reducer;
